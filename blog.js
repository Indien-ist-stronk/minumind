document.addEventListener('DOMContentLoaded', () => {
    const blogForm = document.getElementById('blog-form');
    const blogsDisplay = document.getElementById('blogs-display');

    // Load blogs from localStorage
    const load
